# ════════════════════════════════════════════════════════════════════
# Prettier owns TypeScript, JavaScript, JSON and YAML. It does not own
# SQL or Markdown, and both exclusions are deliberate.
#
# SQL: prettier has no SQL parser, and the migrations are hand-aligned
# in columns anyway — a formatter would not preserve that even if it
# could read them.
#
# MARKDOWN: the specs are hand-wrapped prose full of em-dashes, and
# prettier's table alignment miscounts those characters — it emits
# separator rows two columns wider than the content rows it is meant to
# line up with. It also rewrites `*emphasis*` to `_emphasis_` and pads
# every table cell to the width of the longest one, which turns a
# one-word edit into a whole-table diff. Docs are read by people and by
# agents, not rendered from a pipeline, so hand formatting wins here.
# ════════════════════════════════════════════════════════════════════

**/dist/**
**/build/**
**/coverage/**
**/node_modules/**
pnpm-lock.yaml
*.sql
*.md

# Prettier does **not** read `.gitignore`, which is the whole reason this entry
# has to exist separately. `.pnpm-store/` is the offline package store — 19 MB of
# extracted tarballs — and `prettier --check .` walked into it and reported nine
# style failures in vendored files nobody here wrote. That is not a hypothetical:
# it turned a UI branch's `pnpm format:check` red and cost a review cycle working
# out that the branch's own code was clean. Ignoring a path in git and ignoring it
# for formatting are two separate lists; keep them in step.
.pnpm-store/

# MSW's service worker is generated by `msw init` and committed on purpose (the
# browser fetches it from the server root, so it cannot be resolved from
# node_modules, and a postinstall step that writes into a served directory is
# worse than a checked-in file). It is not ours to format: prettier reflows it,
# the next `msw init` after an msw upgrade writes it back, and the diff is a few
# hundred lines of churn with no change in behaviour.
apps/web/public/mockServiceWorker.js
