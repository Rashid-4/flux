{
  "name": "@flux/db-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Integration tests for the database contract: tenant isolation, role privileges, append-only history, the audit hash chain, and the transactional outbox. These assert the invariants no unit test can reach, because they are enforced by Postgres rather than by TypeScript.",
  "scripts": {
    "test:integration": "vitest run",
    "test:integration:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/pg": "^8.11.10",
    "pg": "^8.13.0",
    "typescript": "^5.6.0",
    "vitest": "^4.0.0"
  }
}
