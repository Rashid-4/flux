{
  "name": "@flux/mocks",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic fixtures derived from @flux/contracts. Lets the UI be built and tested before the API exists, with data guaranteed to have the shape the API will return.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src"
  },
  "dependencies": {
    "@flux/contracts": "workspace:*",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
